'use strict';

describe('myApp.cartView module', function() {

  beforeEach(module('myApp.cartView module'));

  describe('view1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var cartViewCtrl = $controller('cartViewCtrl');
      expect(cartViewCtrl).toBeDefined();
    }));

  });
});