'use strict';

describe('myApp.accountView module', function() {

  beforeEach(module('myApp.accountView'));

  describe('view2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      let view2Ctrl = $controller('accountViewCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});